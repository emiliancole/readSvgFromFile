# readSvgFromFile
Read svg data from text file and render.

- readSvgFromFile.php

This program can read, from a number of defined text files (eg. circle.txt, rect.txt), the svg parameters and render the svg image.

- svgFromShapeSwitch.php

This program can read from a single text file (eg. shapes.txt) the svg parameters and render the svg image.

